% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_outputs.R
\name{summarize_policy_exposure}
\alias{summarize_policy_exposure}
\title{Summarize Policy Exposure}
\usage{
summarize_policy_exposure(
  jurisdiction_summary,
  mpa_track,
  priority_track,
  transboundary_summary
)
}
\arguments{
\item{jurisdiction_summary}{A data frame produced by
`calc_time_in_jurisdictions()`. Must contain `bird_id`, `jurisdiction`,
and `total_hours`.}

\item{mpa_track}{An `sf` object or data frame of track points labeled by
`overlay_mpas()`. Must contain `bird_id`, `Date`, `Time`, and `in_mpa`.}

\item{priority_track}{An `sf` object or data frame of track points labeled by
`overlay_priority_areas()`. Must contain `bird_id`, `Date`, `Time`, and
`in_priority_area`.}

\item{transboundary_summary}{A data frame produced by
`calc_transboundary_movements()`. Must contain `bird_id`,
`is_transboundary`, and `crossed_into_abnj`.}
}
\value{
A data frame with one row per bird summarizing jurisdiction,
  MPA, priority-area, and transboundary exposure metrics.
}
\description{
Creates a policy-facing summary of each bird's exposure to jurisdictions,
marine protected areas, and candidate conservation priority areas.
}
