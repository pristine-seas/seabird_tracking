% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean-regularize.R
\name{regularize_tracks}
\alias{regularize_tracks}
\title{Regularize GPS tracks to a common time interval}
\usage{
regularize_tracks(
  df,
  id_col = "ID",
  date_col = "Date",
  time_col = "Time",
  lat_col = "Latitude",
  lon_col = "Longitude",
  interval_minutes = 30,
  tolerance_minutes = NULL
)
}
\arguments{
\item{df}{A data.frame with tracking data}

\item{id_col}{Column name for bird ID}

\item{date_col}{Column name for date}

\item{time_col}{Column name for time}

\item{lat_col}{Column name for latitude}

\item{lon_col}{Column name for longitude}

\item{interval_minutes}{Target time interval in minutes for the regularized grid}

\item{tolerance_minutes}{Half-window in minutes around each grid point within
which an observed fix is considered a match. Defaults to half the interval.}
}
\value{
A data.frame with one row per bird per regular time step, containing:
  \itemize{
    \item The id column
    \item \code{datetime_regular} – the regular grid timestamp (POSIXct UTC)
    \item Latitude and longitude (NA where no fix matched the grid point)
    \item \code{is_observed} – TRUE if a real fix was matched, FALSE if grid point is empty
  }
}
\description{
Resamples each individual's track to a uniform time step for comparability
across birds. The output is a regular time grid per bird with NA coordinates
wherever no fix fell within the tolerance window around each grid point.
}
