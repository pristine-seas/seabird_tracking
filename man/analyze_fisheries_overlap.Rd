% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_fisheries_overlap.R
\name{analyze_fisheries_overlap}
\alias{analyze_fisheries_overlap}
\title{Analyze Fisheries Overlap}
\usage{
analyze_fisheries_overlap(
  track_data,
  fisheries_data,
  gear_weights = NULL,
  track_id_col = "track_id",
  track_lon_col = "longitude",
  track_lat_col = "latitude",
  fisheries_lon_col = "longitude",
  fisheries_lat_col = "latitude",
  effort_col = "effort",
  gear_col = "gear",
  cell_id_col = NULL,
  join_type = c("intersects", "within", "nearest"),
  crs = 4326,
  standardize_effort = TRUE,
  log_transform = FALSE,
  overlap_effort_col = "effort_std",
  calculate_risk = TRUE,
  scale_risk_01 = TRUE,
  calculate_diel = FALSE,
  track_diel_col = "diel_period",
  fisheries_diel_col = "diel_period"
)
}
\arguments{
\item{track_data}{A data frame or sf object containing seabird track points.}

\item{fisheries_data}{A data frame or sf object containing fisheries effort
points, grid cells, or polygons.}

\item{gear_weights}{Optional named numeric vector or data frame of gear
weights. Required only if `calculate_risk = TRUE`.}

\item{track_id_col}{Character. Column identifying each bird or track.}

\item{track_lon_col}{Character. Longitude column in `track_data` if not sf.}

\item{track_lat_col}{Character. Latitude column in `track_data` if not sf.}

\item{fisheries_lon_col}{Character. Longitude column in `fisheries_data` if
not sf.}

\item{fisheries_lat_col}{Character. Latitude column in `fisheries_data` if
not sf.}

\item{effort_col}{Character. Raw fisheries effort column.}

\item{gear_col}{Character. Fishing gear column.}

\item{cell_id_col}{Optional character. Fisheries grid/cell ID column.}

\item{join_type}{Character. One of `"intersects"`, `"within"`, or `"nearest"`.}

\item{crs}{Coordinate reference system for non-sf inputs. Default is 4326.}

\item{standardize_effort}{Logical. If `TRUE`, creates `effort_std`.}

\item{log_transform}{Logical. If `TRUE`, creates `effort_log`.}

\item{overlap_effort_col}{Character. Effort column used for overlap metrics.
Default is `"effort_std"`.}

\item{calculate_risk}{Logical. If `TRUE`, runs `calc_risk_index()`.}

\item{scale_risk_01}{Logical. If `TRUE`, rescales risk index to [0, 1].}

\item{calculate_diel}{Logical. If `TRUE`, runs `calc_diel_overlap()`.}

\item{track_diel_col}{Character. Diel-period column from track data.}

\item{fisheries_diel_col}{Character. Diel-period column from fisheries data.}
}
\value{
A named list containing standardized fisheries data, sf inputs,
  joined overlap data, overlap metrics, gear summaries, risk results, and
  diel overlap results where requested.
}
\description{
Runs the fisheries overlap workflow from prepared seabird tracking data and
fisheries effort data. This wrapper standardizes fisheries effort, converts
inputs to sf objects when needed, spatially joins tracks to fisheries data,
calculates overlap metrics, optionally calculates risk scores, and optionally
calculates diel overlap.
}
