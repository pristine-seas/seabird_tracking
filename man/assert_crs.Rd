% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R, R/utils_validation.R
\name{assert_crs}
\alias{assert_crs}
\title{Assert that two spatial objects share the same CRS}
\usage{
assert_crs(x, expected_crs = NULL)

assert_crs(x, expected_crs = NULL)
}
\arguments{
\item{x}{An `sf` or `Spatial*` object.}

\item{expected_crs}{Optional. A CRS to compare against. Accepts anything
that \code{sf::st_crs()} can parse: an EPSG integer
(e.g. \code{4326}), a PROJ string, or an \code{crs}
object.}

\item{y}{An sf object.}
}
\value{
Invisible TRUE if CRS matches.

Invisibly returns TRUE if all checks pass.
}
\description{
Checks whether two sf objects have the same coordinate reference system.

Verifies that a spatial object has a CRS assigned and, optionally, that it
matches an expected CRS. Useful for catching mismatched projections before
spatial operations.
}
\examples{
library(sf)
pt <- st_sf(geometry = st_sfc(st_point(c(0, 0)), crs = 4326))
assert_crs(pt)               # passes – CRS is set
assert_crs(pt, 4326)         # passes – CRS matches
assert_crs(pt, 32632)        # errors – CRS mismatch
}
