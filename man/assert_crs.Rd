% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_validation.R
\name{assert_crs}
\alias{assert_crs}
\title{Assert Coordinate Reference System (CRS)}
\usage{
assert_crs(x, expected_crs = NULL)
}
\arguments{
\item{x}{An \code{sf} or \code{Spatial*} object.}

\item{expected_crs}{Optional. A CRS to compare against. Accepts anything
that \code{sf::st_crs()} can parse: an EPSG integer (e.g. \code{4326}),
a PROJ string, or a \code{crs} object.}
}
\value{
Invisibly returns \code{TRUE} if all checks pass.
}
\description{
Verifies that a spatial object has a CRS assigned and, optionally, that it
matches an expected CRS. Useful for catching mismatched projections before
spatial operations.
}
\examples{
library(sf)
pt <- st_sf(geometry = st_sfc(st_point(c(0, 0)), crs = 4326))
assert_crs(pt)
assert_crs(pt, 4326)
\dontrun{
assert_crs(pt, 32632)
}
}
